\begin{document}
\chapter{SILE 源文件与基本排版}

\section{命令、选项与内容}

SILE 的输入语法由反斜杠命令、方括号选项和花括号内容组成。例如下面的命令把一段文字设置为 14 磅：

\begin[type=verbatim]{raw}
\font[size=14pt]{一段文字}
\end{raw}

环境适合包围较长内容。\fdu:code{document} 环境创建文档，\fdu:code{fdu:abstract} 环境创建中文摘要。百分号开始一行注释。源文件使用 UTF-8 编码，中文标点可以直接输入。

\section{文档与分文件}

主文件的最小骨架如下。class 选项指定本项目的文档类，type 与 degree 决定封面类别。

\begin[type=verbatim]{raw}
\begin[class=fduthesis,
  type=master,
  degree=academic]{document}
\fdu:title{论文题目}
\fdu:makecover
\fdu:mainmatter
\chapter{绪论}
正文内容。
\end{document}
\end{raw}

实际论文宜按章拆分，并在主文件中使用 include 命令引入 \fdu:code{src/c01-introduction.sil}。本项目中的分文件也使用 document 环境，SILE 在 include 时会处理其中内容，而不会重新建立文档类。

\section{段落与间距}

空行结束一个段落。模板默认首行缩进 2 em、正文小四号、基线间距 20 磅，不需要用全角空格手工缩进。noindent 命令必须出现在段落内容之前；smallskip、medskip 与 bigskip 命令可以插入少量竖直间距，但正文通常应依赖统一样式。

\section{字体与强调}

正文中的拉丁文字和数字保持 XITS，汉字回退到 FandolSong；标题使用 FandolHei。模板另提供 \fdu:code{fdu:kaiti}、\fdu:code{fdu:fangsong}、\fdu:code{fdu:bold}、\fdu:code{fdu:italic} 与 \fdu:code{fdu:code}。

\fdu:kaiti{这是一段楷体示例，适合中文引文或有限度的强调。}

英文术语可写成 \fdu:italic{OpenType shaping}，内联文件名可写成 \fdu:code{paper.sil}。不要在章节正文中随意改字号或字体，以免破坏规范的一致性。

\section{列表}

模板加载了 lists 包。有序列表与无序列表不需要手工维护编号：

\begin{enumerate}
\item{先填写主文件中的论文元数据。}
\item{再按章节编辑 src 目录中的源文件。}
\item{最后运行三轮编译并检查 PDF。}
\end{enumerate}

\begin{itemize}
\item{列表项可以包含中文和英文；}
\item{嵌套列表应节制使用；}
\item{论文中的论证通常仍以完整段落为主。}
\end{itemize}

对应源代码是：

\begin[type=verbatim]{raw}
\begin{enumerate}
  \item{第一项}
  \item{第二项}
\end{enumerate}
\end{raw}

\section{代码块与网址}

教程中的源码块使用 verbatim 类型的 raw 环境。raw 处理器保留反斜杠和花括号，verbatim 处理器保留换行与空格。下面给出一个适合放进这种环境的多行片段：

\begin[type=verbatim]{raw}
\font[family=XITS,size=12pt]{
  Latin text and 12345
}
\end{raw}

长代码行容易超出版心，应主动换行。本模板将代码字号设为 8.5 磅并使用 TeX Gyre Cursor，但它不会替作者自动重排程序结构。

\section{脚注}

规范建议需要补充说明时优先使用脚注。写作时在正文位置调用脚注命令即可\footnote{脚注会排在当前页底部，并随正文分页。}：

\begin[type=verbatim]{raw}
正文中的论断\footnote{补充说明。}
\end{raw}

模板在每章开始时重置脚注计数。脚注不应承担本应进入正文的关键论证，也不能代替参考文献著录。

\section{本章小结}

SILE 源文件把内容与命令组合起来；本模板进一步把学校要求封装为高层命令。下一章说明这些命令如何构成一篇完整论文。
\end{document}
