\begin{document}

\chapter{高级功能与常见问题}

\section{模板定制}

\subsection{修改论文类型}

本模板通过文档开头的 class 选项来指定论文类型。type 参数可选值包括 doctor（博士论文，默认值）和 master（硕士论文）。degree 参数可选值包括 academic（学术学位，默认值）和 professional（专业学位）。

不同的类型组合会影响封面的显示内容：博士学术学位论文显示"博士学位论文（学术学位）"；博士专业学位论文显示"博士学位论文（专业学位）"，且"专业学位类别（领域）"替代"专业"字段；硕士论文的显示规则与博士论文类似。

\subsection{修改页面布局}

模板默认使用 A4 纸张（210mm × 297mm），页边距设置为上下各 25.4mm、左右各 31.8mm。页眉区域位于距页面顶部 14mm 至 30mm 之间，正文区域从距顶部 35mm 处开始。

如需调整页面布局，可以在 fduthesis.lua 文件中修改 defaultFrameset 的相关参数。修改时要确保满足复旦大学论文规范的要求。

\subsection{修改字体设置}

如果系统中使用的字体名称与模板默认的不同，可以在 fduthesis.lua 中修改对应的字体名称。例如，在某些 Linux 发行版中，宋体可能名为"Noto Serif CJK SC"而非"SimSun"，黑体可能名为"Noto Sans CJK SC"而非"SimHei"。

修改字体设置时，需要同时更新文档类中所有使用该字体的位置，包括正文字体、标题字体、页眉字体等。

\section{SILE 命令速查}

\subsection{文档结构命令}

以下是常用的文档结构命令：

（1）chapter 命令：创建新章，自动编号并添加到目录。格式为"第X章　章标题"。

（2）section 命令：创建新节，自动编号。格式为"X.Y　节标题"。

（3）subsection 命令：创建新小节，自动编号。格式为"X.Y.Z　小节标题"。

\subsection{排版命令}

以下是常用的排版命令：

（1）par：段落分隔，开始新段落。

（2）noindent：取消当前段落的首行缩进。

（3）center：居中排版环境。

（4）raggedleft：右对齐排版。

（5）bigskip、medskip、smallskip：添加不同大小的垂直间距。

（6）vfill：添加弹性垂直空间，将后续内容推向页面底部。

（7）hrule：绘制水平线，可指定宽度和粗细。

\subsection{字体命令}

使用 font 命令可以设置字体的各种属性：

（1）family：字体族名称，如 SimSun、SimHei、Times New Roman 等。

（2）size：字号大小，如 12pt、14pt、16pt 等。

（3）weight：字重，如 400（常规）、700（粗体）等。

（4）style：字形，如 italic（斜体）等。

\subsection{图片命令}

img 命令用于插入图片：

（1）src：图片文件路径（必需）。

（2）width：图片显示宽度。

（3）height：图片显示高度。

\section{常见问题}

\subsection{编译错误处理}

如果编译过程中出现错误，请首先检查以下几点：

（1）确保 SILE 版本为 0.15.x 或更高版本。版本过低可能不支持模板使用的某些功能。

（2）确保所有必需的字体已正确安装在系统中。可以在终端中运行 fc-list 命令查看已安装的字体列表。

（3）检查源文件中的命令拼写是否正确，特别注意大小写和参数格式。SILE 的命令名称区分大小写。

（4）确保所有引用的文件路径正确无误。相对路径以主文件 paper.sil 所在目录为基准。

\subsection{目录页码不正确}

目录中的页码需要两次编译才能正确显示。第一次编译时，SILE 会生成 paper.toc 文件记录各章节的页码信息；第二次编译时，才会将这些页码信息填入目录页面。

如果使用 make 命令编译，会自动执行两次编译。如果手动编译，请确保执行两次 sile paper.sil 命令。

\subsection{字体显示问题}

如果某些字体无法正常显示，请按以下步骤排查：

（1）确认字体已安装：使用 fc-list 命令检查系统中是否有对应字体。

（2）确认字体名称：字体名称必须与系统中注册的名称完全匹配，可以使用 fc-list 查看准确名称。

（3）刷新字体缓存：在 Linux 系统上运行 fc-cache -fv 命令刷新字体缓存。

\subsection{图片无法显示}

如果图片无法正常显示，请检查以下问题：

（1）图片文件路径是否正确。路径相对于主文件 paper.sil 所在目录。

（2）图片格式是否被 SILE 支持。推荐使用 PDF、PNG 或 JPG 格式。

（3）图片文件是否存在于指定路径。

\section{模板文件说明}

本模板包含以下主要文件：

（1）paper.sil：主文件，定义论文整体结构和元数据设置。

（2）classes/fduthesis.lua：文档类定义文件，包含所有排版格式规则。

（3）src/ 目录：各章节的源文件，通过 include 命令引入主文件。

（4）Makefile：构建脚本，实现自动化两次编译。

（5）fudan-name.pdf：复旦大学校名矢量图片，用于封面。

修改论文内容时，主要编辑 src 目录下的各章节文件即可。修改论文结构或元数据时，编辑 paper.sil 文件。修改排版格式时，编辑 classes/fduthesis.lua 文件。

\section{本章小结}

本章介绍了模板的高级定制功能、SILE 命令速查以及常见问题的解决方法。通过本章的内容，使用者可以根据自己的需求对模板进行定制，并能够解决使用过程中遇到的常见问题。

\end{document}
