\begin{document}

\chapter{模板简介与快速入门}

\section{模板概述}

本模板是为复旦大学博士和硕士学位论文设计的 SILE 排版模板。SILE（Simon's Improved Layout Engine）是一个现代化的排版系统，它继承了 TeX 优秀的断行和分页算法，同时使用 Lua 语言作为其脚本引擎，提供了更灵活的定制能力。

与传统的 LaTeX 模板相比，SILE 模板具有以下优势：

（1）使用 Lua 语言进行扩展，编程能力更强，定制更灵活。

（2）原生支持 Unicode 和 OpenType 字体，对中文排版有良好的支持。

（3）语法更简洁直观，学习曲线相对平缓。

（4）排版质量与 TeX 引擎相当，尤其在断行算法方面表现优秀。

\section{系统要求}

使用本模板需要满足以下系统要求：

（1）SILE 排版引擎 0.15.x 或更高版本。

（2）中文字体：宋体（SimSun）、黑体（SimHei）、楷体（KaiTi）、仿宋（FangSong）。

（3）英文字体：Times New Roman。

（4）等宽字体：Hack（用于代码显示，可选）。

（5）GNU Make（用于自动化编译，可选但推荐）。

\section{安装与配置}

\subsection{安装 SILE}

SILE 可以通过多种方式安装。在 Linux 系统上，可以通过包管理器安装；在 macOS 上，可以使用 Homebrew 安装；也可以从源代码编译安装。详细的安装说明请参阅 SILE 官方文档。

\subsection{获取模板}

本模板的源代码托管在 GitHub 上，可以通过 Git 克隆获取最新版本。获取模板后，模板的目录结构如下：项目根目录包含主文件 paper.sil 和 Makefile 构建脚本；classes 目录包含文档类定义文件 fduthesis.lua；src 目录包含各章节的源文件。

\subsection{字体配置}

确保系统中安装了所需的中英文字体。在 Linux 系统上，可以将字体文件放置在系统字体目录或用户字体目录中。在 macOS 系统上，可以通过"字体册"应用安装字体。如果字体名称与默认设置不同，可以在 fduthesis.lua 中修改对应的字体名称。

\section{编译论文}

\subsection{使用 Make 编译}

在模板根目录下，运行 make 命令即可编译论文。编译过程自动执行两次 SILE 编译：第一次生成目录数据文件（paper.toc），第二次将正确的页码填入目录中。

\subsection{手动编译}

如果不使用 Make，也可以手动执行编译命令。需要在命令行中依次执行两次 sile paper.sil 命令。第一次编译后会生成目录数据文件，第二次编译后目录中的页码才是正确的。

\section{文档结构说明}

本模板的主文件为 paper.sil，其基本结构如下：首先在文档开头通过 class 选项指定文档类型（doctor 或 master）和学位类型（academic 或 professional），然后设置论文元数据（标题、作者、导师等），接着依次生成封面、声明页、前置部分（包括目录和摘要）、正文部分（各章节）和后置部分（参考文献、附录、致谢）。

各章节内容分别存放在 src 目录下的独立文件中，通过 include 命令引入主文件。这种模块化的组织方式使得各章节可以独立编辑，便于多人协作和版本管理。

\section{本章小结}

本章介绍了模板的基本信息、系统要求、安装配置方法和编译流程。在下一章中，我们将详细介绍模板提供的基本排版功能。

\end{document}
