\begin{document}
\chapter{规范依据与快速开始}

\section{模板覆盖范围}

本模板直接依据复旦大学研究生院发布的《复旦大学博士、硕士学位论文规范（2026 年 6 月修订版）》制作，覆盖学术学位与专业学位的硕士、博士论文。文档类保留了 bachelor 选项作为技术扩展，但本科毕业设计有独立管理要求，不能把该选项理解为已经满足本科规范。

规范要求论文依次包含封面、指导小组成员名单、目录、中英文摘要、正文、参考文献、致谢，以及末尾的独创性声明和使用授权声明；有图或表时，还必须分别提供插图清单和附表清单。本示例完整走通了这一顺序。

\section{安装检查}

本项目针对 SILE 0.15 系列实现。在项目根目录执行下列命令检查版本：

\begin[type=verbatim]{raw}
sile --version
FONTCONFIG_FILE="$PWD/config/fontconfig-texlive.conf" fc-match XITS
FONTCONFIG_FILE="$PWD/config/fontconfig-texlive.conf" fc-match FandolSong
FONTCONFIG_FILE="$PWD/config/fontconfig-texlive.conf" fc-match FandolHei
\end{raw}

模板默认使用 XITS、FandolSong、FandolHei、FandolKai、FandolFang、TeX Gyre Heros 和 TeX Gyre Cursor，与 fduthesis-2026 在 Linux 下的默认字面保持一致。项目私有 \fdu:code{config/fontconfig-texlive.conf} 直接暴露 TeX Live 字体目录，并修正 Fandol Regular/Bold 的 fontconfig 权重。若环境不同，可集中修改该配置和 \fdu:code{classes/fduthesis.lua} 顶部的 \fdu:code{FONTS} 表。

\section{第一次编译}

推荐直接执行：

\begin[type=verbatim]{raw}
make
\end{raw}

目录、插图清单和附表清单依赖上一轮编译记录的页码。分页又可能因目录长度变化而改变，因此 Makefile 固定运行三次 SILE；手动编译时也应连续执行三次：

\begin[type=verbatim]{raw}
FONTCONFIG_FILE="$PWD/config/fontconfig-texlive.conf" sile paper.sil
FONTCONFIG_FILE="$PWD/config/fontconfig-texlive.conf" sile paper.sil
FONTCONFIG_FILE="$PWD/config/fontconfig-texlive.conf" sile paper.sil
\end{raw}

生成的主文件是 \fdu:code{paper.pdf}。不要只检查编译退出码，还应打开 PDF 检查标题是否换行得当、图表是否越界、空白页是否合理，以及双面打印时正文是否从右页开始。

\section{项目布局}

主文件 \fdu:code{paper.sil} 决定整体顺序并填写元数据；\fdu:code{classes/fduthesis.lua} 是排版类；\fdu:code{src} 目录保存摘要、章节与后置材料；\fdu:code{assets/fudan-name.pdf} 是封面使用的校名矢量图。通常只需编辑主文件与 \fdu:code{src} 下的内容，除非确实需要改变学校级版式。

\section{规范中的人工检查项}

模板能执行形式约束，却不能替代内容审查。论文题目原则上不超过 30 个汉字；中文摘要通常为 300 至 1000 字，关键词为 3 至 8 个；参考文献按提交时现行的 GB/T 7714 著录。涉密论文必须填写密级与年限，公开论文则不应显示密级。最终稿按要求双面打印，并采用线装或热胶装订。

\section{本章小结}

使用者应先确认论文类别、字体与 SILE 版本，再通过三轮编译得到稳定 PDF。下一章介绍构成本教程的 SILE 语法。
\end{document}
